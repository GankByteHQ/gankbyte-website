(()=>{const input=document.getElementById("docs-search"),cards=[...document.querySelectorAll("[data-doc]")],count=document.getElementById("docs-count"),empty=document.getElementById("docs-empty");if(!input)return;function filter(){const query=input.value.trim().toLowerCase();let visible=0;cards.forEach(card=>{const match=!query||card.textContent.toLowerCase().includes(query);card.hidden=!match;if(match)visible+=1});count.textContent=`${visible} guide${visible===1?"":"s"}`;empty.hidden=visible!==0}input.addEventListener("input",filter);filter()})();
