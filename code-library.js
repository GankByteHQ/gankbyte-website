(()=>{const cards=[...document.querySelectorAll("[data-snippet-card]")],filters=[...document.querySelectorAll("[data-filter]" )],search=document.getElementById("snippet-search"),empty=document.getElementById("snippet-empty");let active="all";function apply(){const query=search.value.trim().toLowerCase();let visible=0;cards.forEach(card=>{const matchesFilter=active==="all"||card.dataset.language.split(" ").includes(active),matchesSearch=!query||card.textContent.toLowerCase().includes(query);card.hidden=!(matchesFilter&&matchesSearch);if(!card.hidden)visible+=1});empty.hidden=visible!==0}filters.forEach(button=>button.addEventListener("click",()=>{active=button.dataset.filter;filters.forEach(item=>item.classList.toggle("active",item===button));apply()}));search.addEventListener("input",apply);async function copy(text){try{await navigator.clipboard.writeText(text);return true}catch{const area=document.createElement("textarea");area.value=text;document.body.append(area);area.select();const copied=document.execCommand("copy");area.remove();return copied}}cards.forEach(card=>card.querySelector("[data-copy-snippet]").addEventListener("click",async event=>{const button=event.currentTarget,original=button.textContent,ok=await copy(card.querySelector("code").textContent);button.textContent=ok?"Copied":"Copy failed";setTimeout(()=>{button.textContent=original},1400)}));apply()})();
