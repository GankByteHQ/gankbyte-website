(() => {
  "use strict";
  const $ = (id) => document.getElementById(id), form = $("readme-form"), output = $("readme-output"), status = $("readme-status"); let current = "Generate a README to see it here.";
  const v = (id) => $(id).value.trim();
  function build() { const name = v("readme-name") || "Untitled Project", kind = v("readme-kind"), description = v("readme-description") || "A useful project.", license = v("readme-license"), install = v("readme-install") || "Follow the installation steps below.", run = v("readme-run") || "Start the project using the command for your platform.", deps = v("readme-deps") || "None beyond the target platform.", notes = v("readme-notes") || "Add commands, exports, examples, and screenshots here."; return [`# ${name}`, "", description, "", `> Project type: ${kind}`, "", "## Features", "", "- Focused scope with readable source", "- Configuration kept separate from core logic", "- Practical output that can be reviewed before publishing", "", "## Requirements", "", deps.split(",").map((item) => `- ${item.trim()}`).join("\n"), "", "## Installation", "", "```bash", install, "```", "", "## Usage", "", "```text", run, "```", "", "## Commands, exports, and examples", "", notes, "", "## Configuration", "", "Document every required environment variable, config field, permission, dependency, and default value here.", "", "## Troubleshooting", "", "1. Check the requirements and dependency versions.", "2. Read the first error in the console, not only the final cascade.", "3. Confirm file paths, permissions, and configuration values.", "4. Open an issue with the version, steps to reproduce, and relevant logs with secrets removed.", "", "## Contributing", "", "Pull requests and focused issue reports are welcome. Explain what changed, how it was checked, and any compatibility impact.", "", "## Licence", "", `This project is released under **${license}**. Replace this section with the complete licence text or a link to the correct licence file before publishing.`, "", "## Status", "", "This project is actively maintained only when the repository says so. Check the changelog for current release information.", ""].join("\n"); }
  form.addEventListener("submit", (event) => { event.preventDefault(); current = build(); output.textContent = current; status.textContent = "README generated locally."; });
  $("reset-readme").addEventListener("click", () => { form.reset(); current = "Generate a README to see it here."; output.textContent = current; status.textContent = "README form reset."; });
  $("copy-readme").addEventListener("click", async () => { try { await navigator.clipboard.writeText(current); status.textContent = "README copied to the clipboard."; } catch { status.textContent = "Clipboard access was unavailable."; status.classList.add("is-error"); } });
  $("download-readme").addEventListener("click", () => { const url = URL.createObjectURL(new Blob([current], {type:"text/markdown;charset=utf-8"})); const link = document.createElement("a"); link.href = url; link.download = "README.md"; link.click(); URL.revokeObjectURL(url); status.textContent = "README downloaded."; });
})();
